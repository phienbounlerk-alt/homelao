{
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 flutter_sdk && export PATH=\"$PATH:$(pwd)/flutter_sdk/bin\" && flutter config --enable-web && flutter pub get && flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "echo skip"
}
